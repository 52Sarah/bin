# bin

Helper and utility scripts, etc. Generally written in bash. 

Generally located in `$HOME/bin`.

And added to the end of $PATH in .bash_profile.



## heading 2

### heading 3

- bullets
- bullets

* bullets
* bullets

1. numbered
2. numbered

```sql
	SELECT *
	FROM tbl t
	WHERE t.id = 42
	ORDER BY 1;
```